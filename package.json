{
  "name": "polycheck",
  "version": "1.2.0",
  "description": "Extract Polymarket contender birth dates for horoscope analysis",
  "scripts": {
    "dev": "netlify dev",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "node --test dist/tests/**/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^2.4.0",
    "@netlify/blobs": "^8.1.0",
    "node-fetch": "^3.3.2",
    "compromise": "^14.14.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
